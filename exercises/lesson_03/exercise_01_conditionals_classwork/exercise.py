def classify_grade(score):
    """
    Print a single letter grade using an if/elif/else chain:
    "A" for 90+, "B" for 80-89, "C" for 70-79, "F" otherwise.

    Example:
    classify_grade(82) -> prints: B
    """
    # TODO: replace the line below with your solution
    pass


def range_checks(temperature, age):
    """
    Using a chained comparison (60 <= temperature <= 80), print
    "Comfortable" if true, otherwise "Not comfortable". Then, using
    another chained comparison, print 13 <= age <= 19 directly (this
    prints True or False).

    Example:
    range_checks(72, 15) -> prints:
    Comfortable
    True
    """
    # TODO: replace the line below with your solution
    pass


def membership_checks(fruits, word):
    """
    Using in/not in, print (one per line): whether "banana" is in
    fruits, whether "grape" is not in fruits, and whether "y" is in
    word.

    Example:
    membership_checks(["apple", "banana", "mango"], "python") -> prints:
    True
    True
    True
    """
    # TODO: replace the line below with your solution
    pass
