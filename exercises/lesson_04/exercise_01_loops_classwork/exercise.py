def sum_even_numbers(start, end):
    """
    Use a for loop with range() to add up every even number from start
    to end (inclusive). Print the total using an accumulator variable.

    Example:
    sum_even_numbers(2, 20) -> prints: Sum of even numbers 2 to 20: 110
    """
    # TODO: replace the line below with your solution
    pass


def countdown_with_break(limit, stop_at):
    """
    Starting a counter at 1, use a while loop that keeps looping while
    the counter is less than or equal to limit. Inside the loop, print
    the counter value. The moment the counter equals stop_at, print
    "Loop stopped early!" and use break to exit the loop immediately.

    Example:
    countdown_with_break(5, 3) -> prints:
    1
    2
    3
    Loop stopped early!
    """
    # TODO: replace the line below with your solution
    pass


def numbered_shopping_list(items):
    """
    Use enumerate() to print each item in items with its position
    number starting at 1, in the format "N) item".

    Example:
    numbered_shopping_list(["Milk", "Eggs", "Bread", "Apples"]) -> prints:
    1) Milk
    2) Eggs
    3) Bread
    4) Apples
    """
    # TODO: replace the line below with your solution
    pass
