import math
import random


def circle_area(radius):
    """
    Import the math module (already done at the top of this file).
    Return math.pi multiplied by radius squared.

    Example:
    circle_area(4) -> returns: 50.26548245743669
    """
    # TODO: replace the line below with your solution
    pass


def sqrt_of(value):
    """
    Return the square root of value using math.sqrt().

    Example:
    sqrt_of(144) -> returns: 12.0
    """
    # TODO: replace the line below with your solution
    pass


def dice_rolls(seed, n):
    """
    Call random.seed(seed) so results are repeatable. Then use
    random.randint(1, 6) to simulate rolling a six sided die n times,
    returning the results as a list. Because you seeded with the same
    number, calling this again with the same seed and n should return
    the exact same list.

    Example:
    dice_rolls(7, 5) -> returns: [3, 2, 4, 6, 1]
    """
    # TODO: replace the line below with your solution
    pass


def format_date(dt):
    """
    dt is a datetime.datetime object. Use strftime() to return it
    formatted as "Month Day, Year" (for example "July 08, 2026") using
    the format string "%B %d, %Y".

    Example:
    format_date(datetime.datetime(2026, 7, 8)) -> returns: "July 08, 2026"
    """
    # TODO: replace the line below with your solution
    pass
