def list_cleanup(numbers):
    """
    Append 50 to the end of numbers, insert 5 at index 0, and remove 99
    from the list. Return the final list.

    Example:
    list_cleanup([12, 99, 30, 7]) -> returns: [5, 12, 30, 7, 50]
    """
    # TODO: replace the line below with your solution
    pass


def coordinates_unpacking(point):
    """
    Unpack the tuple point into three variables x, y, and z in a
    single line, then return a sentence reporting all three values in
    the format "x=X, y=Y, z=Z".

    Example:
    coordinates_unpacking((4, -2, 9)) -> returns: "x=4, y=-2, z=9"
    """
    # TODO: replace the line below with your solution
    pass


def contact_card(name, phone, updated_phone, email):
    """
    Create a dictionary contact with keys 'name' and 'phone' from name
    and phone. Add a new key 'email' set to email. Update the 'phone'
    value to updated_phone. Use .get() to safely look up a key
    'address' that doesn't exist, defaulting to 'Not on file'. Return
    a tuple of (address_lookup, contact).

    Example:
    contact_card("Priya", "555-0101", "555-0199", "priya@example.com")
    -> returns: ("Not on file", {"name": "Priya", "phone": "555-0199", "email": "priya@example.com"})
    """
    # TODO: replace the line below with your solution
    pass
